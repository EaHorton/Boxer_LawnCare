name: GitHub Pagesname: GitHub Pages

on:

on:  push:

  push:    branches:

    branches:      - main

      - mainjobs:

  pull_request:  deploy:

    runs-on: ubuntu-latest

jobs:    steps:

  deploy:      - uses: actions/checkout@v3

    runs-on: ubuntu-latest      - uses: peaceiris/actions-hugo@v2

    permissions:        with:

      contents: write          hugo-version: '0.152.2'

    steps:          extended: true

      - uses: actions/checkout@v3      - name: Build

        with:        run: hugo --minify

          submodules: true      - name: Deploy

          fetch-depth: 0        uses: peaceiris/actions-gh-pages@v3

        with:

      - name: Setup Hugo          github_token: ${{ secrets.GITHUB_TOKEN }}

        uses: peaceiris/actions-hugo@v2          publish_dir: ./public

        with:
          hugo-version: '0.152.2'
          extended: true

      - name: Clean public directory
        run: rm -rf public

      - name: Build
        run: |
          hugo --minify
          touch public/.nojekyll

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        if: github.ref == 'refs/heads/main'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          force_orphan: true